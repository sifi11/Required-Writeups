# Forensics Git 2
## How to solve:
I downloaded the disk image and opened it with Autopsy and went to the fourth volume. I then went to `\home\ctf-player\Code` and found the `killer-chat-app` folder which I extracted. I then went to the directory where I extracted it and ran `git log`, but no flag was there and no commits were there either, so I looked around a little bit and couldn't find anything. I looked back at the challenge and saw something about someone interrupting deletion. So I asked Claude how I would access Git files that weren't deleted, and he said to use `git fsck --unreachable` to see what git blobs or trees weren't reachable. I then ran `git fsck --lost-found` to see which ones might be useful. I saw a dangling blob that looked suspicious, so I ran `git log --all <commit hash>` and saw a commit about adding a secret hideout thing. So I ran `git show <commit hash>` and saw someone telling someone about the flag.
